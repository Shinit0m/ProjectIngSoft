import { LocalstoragePipe } from './localstorage.pipe';

describe('LocalstoragePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalstoragePipe();
    expect(pipe).toBeTruthy();
  });
});
